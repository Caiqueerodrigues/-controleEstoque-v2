export const MESSAGES = {
    // 1000: {
    // erro 400 geralmente usado com erro do backend (interno)
    // }

    1010: (field) => ({
        //insira o valor
        code:1010,
        msg:`Por favor, insira a(o) ${field}`
    })
}